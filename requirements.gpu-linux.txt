-c requirements.txt
# Optional Linux/CUDA extras for full training on an NVIDIA machine.
# Install this from the repository root after `requirements.txt` succeeds.
# The constraint line above prevents packages such as DeepSpeed from upgrading
# torch/torchvision/torchaudio beyond the versions used by this codebase.

bitsandbytes==0.41.0
deepspeed==0.10.3
triton==2.0.0

# `flash-attn` is intentionally not pinned here because the correct wheel/build
# depends on your exact CUDA, PyTorch, Python, and GPU toolchain combination.
# Install it manually if needed, e.g.:
#   pip install flash-attn --no-build-isolation
